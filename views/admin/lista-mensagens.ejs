<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Mensagens - Admin Acapulco</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet" />

  <style>
    .message-card {
      background-color: white;
      border-radius: 0.75rem;
      box-shadow: 0 0.25rem 1rem rgba(0,0,0,0.05);
      margin-bottom: 1.5rem;
    }
    .message-header {
      background-color: #f59342;
      color: white;
      padding: 1rem 1.25rem;
      border-top-left-radius: 0.75rem;
      border-top-right-radius: 0.75rem;
    }
    .message-body {
      padding: 1.25rem;
    }
  </style>
</head>
<body class="bg-light">
  <div class="d-flex">
    <%- include('../partials/sidebar-admin') %>

    <div class="flex-fill p-4">
      <div class="container">
        <h2 class="mb-4">Mensagens dos Moradores</h2>

        <% if (mensagens.length === 0) { %>
          <div class="alert alert-info">Nenhuma mensagem recebida até o momento.</div>
        <% } else { %>
          <% mensagens.forEach(msg => { %>
            <div class="message-card">
              <div class="message-header">
                <strong><i class="bi bi-person-fill me-1"></i> <%= msg.nameResident %></strong>
                <span class="ms-3"><i class="bi bi-tag-fill me-1"></i> <%= msg.title %></span>
              </div>
              <div class="message-body">
                <p class="mb-0"><%= msg.content %></p>
              </div>
            </div>
          <% }) %>
        <% } %>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>