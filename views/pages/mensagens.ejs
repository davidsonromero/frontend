<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Mensagens - Morador | Residencial Acapulco</title>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet" />
  <style>
    body {
      background-color: #f8f9fa;
    }
    .card-message {
      border-radius: 1rem;
      box-shadow: 0 0.25rem 0.75rem rgba(0,0,0,0.05);
    }
    .form-section {
      background-color: white;
      padding: 1.5rem;
      border-radius: 1rem;
      box-shadow: 0 0.25rem 0.75rem rgba(0,0,0,0.05);
    }
  </style>
</head>
<body>
  <div class="d-flex">
    <%- include('../partials/sidebar-morador') %>

    <div class="flex-fill p-4">
      <div class="container">
        <h2 class="mb-4">Mensagens para a Administração</h2>

        <!-- Formulário para nova mensagem -->
        <div class="form-section mb-5">
          <h5>Nova Mensagem</h5>
          <form method="POST" action="/dashboard/send-message">
            <div class="mb-3">
              <label for="Title" class="form-label">Tema</label>
              <input type="text" class="form-control" id="Title" name="Title" required>
            </div>
            <div class="mb-3">
              <label for="Content" class="form-label">Mensagem</label>
              <textarea class="form-control" id="Content" name="Content" rows="4" required></textarea>
            </div>
            <button type="submit" class="btn btn-primary">
              <i class="bi bi-send-fill me-1"></i> Enviar Mensagem
            </button>
          </form>
        </div>

        <!-- Mensagens anteriores -->
        <h5 class="mb-3">Mensagens Enviadas</h5>
        <% if (messages.length === 0) { %>
          <div class="alert alert-info">Você ainda não enviou nenhuma mensagem.</div>
        <% } else { %>
          <% messages.forEach(msg => { %>
            <div class="card card-message mb-3">
              <div class="card-header bg-light fw-bold">
                <i class="bi bi-tag-fill me-2 text-primary"></i><%= msg.title %>
              </div>
              <div class="card-body">
                <p class="mb-0"><%= msg.content %></p>
              </div>
            </div>
          <% }) %>
        <% } %>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
